@using DigitalLearningSolutions.Data.Models.Supervisor;
@model SupervisorDelegateDetail;
<div class="searchable-element nhsuk-panel" id="@Model.ID-card">
  <details class="nhsuk-details nhsuk-expander nhsuk-u-margin-bottom-0">
    <summary class="nhsuk-details__summary nhsuk-u-padding-top-0 nhsuk-u-padding-left-0">
      <span class="nhsuk-details__summary-text searchable-element-title" id="@Model.ID-name" name="name" role="heading" aria-level="3">
        @if (Model.CandidateID == null)
        {
          <text>@Model.DelegateEmail</text>
        }
        else
        {
          <text>@Model.FirstName @Model.LastName (@Model.DelegateEmail)</text>
        }
      </span>
    </summary>
    <div class="nhsuk-details__text nhsuk-u-padding-left-0">
      <div class="nhsuk-u-padding-left-3">
        <div class="nhsuk-grid-row">
         details
        </div>
        <div class="nhsuk-grid-row">
         more details
        </div>
       
      </div>
    </div>
  </details>
  @*<div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-full">
      <a class="nhsuk-button nhsuk-u-margin-bottom-0 nhsuk-u-margin-top-0"
         aria-describedby="@Model.ID-name"
         asp-controller="LearningMenu"
         asp-action="Index"
         asp-route-customisationId="@Model.ID">
        Launch course
      </a>
    </div>
  </div>*@
</div>
