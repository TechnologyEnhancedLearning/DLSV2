@using DigitalLearningSolutions.Web.ViewModels.LearningPortal.SelfAssessments
@model SelfAssessmentDescriptionViewModel

@{
  Layout = "SelfAssessments/_Layout";
  ViewData["Title"] = "Self Assessment";
  ViewData["SelfAssessmentTitle"] = @Model.Name;
}
@section breadcrumbs {
  <li class="nhsuk-breadcrumb__item">Self Assessment</li>
}
<h2>@Model.Name</h2>
<p class="nhsuk-u-reading-width">@Model.Description</p>
@if (Model.UseFilteredApi)
{
  <p class="nhsuk-u-reading-width">Once you have completed the self-assessment, your results will be submitted to the Magpie sign-posting tool which will recommend suitable learning modules.</p>
}
@if (Model.UserBookmark != null)
{
<a class="nhsuk-button trigger-loader" href=@Model.UserBookmark >
  Continue
</a>
}
<a class=@(Model.UserBookmark != null ? "nhsuk-button nhsuk-button--secondary" : "nhsuk-button") asp-action="SelfAssessmentCompetency" asp-route-selfAssessmentId="@Model.Id" asp-route-competencyNumber="1">
  Go to Question 1
</a>
