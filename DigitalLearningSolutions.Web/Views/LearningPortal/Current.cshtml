@using DigitalLearningSolutions.Web.ViewModels.LearningPortal
@model CurrentViewModel

@{
  ViewData["Title"] = "Learning Portal - Current";
}

@section styles {
  <link rel="stylesheet" href="@Url.Content("~/css/learningPortal/index.css")">
}

<partial name="_CourseTypeSwitcher"/>

<div class="nhsuk-grid-row">
  <div class="nhsuk-grid-column-full">
    <h1>My Current Courses</h1>
    <div class="nhsuk-grid-row">
      @foreach (var currentCourse in Model.CurrentCourses) {
        <div class="nhsuk-grid-column-one-quarter">
          <div class="nhsuk-panel @currentCourse.CssClassname">
            <p class="headline-figures">
              <span class="nhsuk-u-font-size-32">@currentCourse.Value.ToString("N0")</span>
              <span class="nhsuk-body-s">@currentCourse.Label</span>
            </p>
          </div>
        </div>
      }
    </div>
  </div>
</div>

@section scripts {
  <script src="@Url.Content("~/js/learningPortal/index.js")"></script>
}
