@using DigitalLearningSolutions.Web.ViewModels.LearningPortal.Completed
@model CompletedCourseViewModel

<div class="course-card nhsuk-panel">
  <details class="nhsuk-details nhsuk-expander nhsuk-u-margin-bottom-0">
    <summary class="nhsuk-details__summary nhsuk-u-padding-top-0 nhsuk-u-padding-left-0">
      <span class="nhsuk-details__summary-text course-title" id="@Model.Id-name" name="name" role="heading" aria-level="3">
        @Model.Name
      </span>
    </summary>
    <div class="nhsuk-details__text">
      <div class="nhsuk-grid-row">
        <partial name="Shared/_TagRow" model="@Model" />
      </div>
      <div class="nhsuk-grid-row">
        <partial name="Shared/_Dates" model="@Model" />
      </div>
      <partial name="Shared/_Scores" model="@Model" />
    </div>
  </details>

  <div class="flex-row">
    <a
      class="nhsuk-button nhsuk-u-margin-bottom-0 nhsuk-u-margin-right-2"
      aria-describedby="@Model.Id-name"
      href="@Model.LaunchUrl">
      Launch course
    </a>
    @if (Model.HasFinaliseButton())
    {
      <a
        class="nhsuk-button nhsuk-button--secondary nhsuk-u-margin-bottom-0"
        aria-describedby="@Model.Id-name"
        aria-label="@Model.FinaliseButtonAriaLabel()"
        href="@Model.EvaluateUrl">
        <span aria-hidden="true">
          @Model.FinaliseButtonText()
        </span>
      </a>
    }
  </div>
</div>
