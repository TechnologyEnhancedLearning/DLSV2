@using DigitalLearningSolutions.Web.ViewModels.Common.ViewComponents
@model LogoViewModel
@if (Model.Logo?.LogoUrl != null)
{
  <img class="centre-brand-logo" src="@Model.Logo.LogoUrl" alt="@Model.Logo.Name logo" />
}
