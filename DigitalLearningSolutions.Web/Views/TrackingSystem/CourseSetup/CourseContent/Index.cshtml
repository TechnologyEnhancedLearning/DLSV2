@inject IConfiguration Configuration
@using DigitalLearningSolutions.Web.ViewModels.TrackingSystem.CourseSetup.CourseContent
@using Microsoft.Extensions.Configuration
@model CourseContentViewModel

<link rel="stylesheet" href="@Url.Content("~/css/trackingSystem/courseContent.css")" asp-append-version="true">

@{
  ViewData["Title"] = "Course content";
}

@section NavBreadcrumbs {
  <partial name="/Views/TrackingSystem/CourseSetup/Shared/_BreadcrumbsToManageCoursePage.cshtml" model="Model.CustomisationId" />
}

<div class="nhsuk-grid-row">
  <div class="nhsuk-grid-column-full">

    <h1 class="nhsuk-heading-xl">Course content</h1>

    <div class="nhsuk-grid-row">
      <div class="nhsuk-grid-column-one-quarter">
        <p class="nhsuk-lede-text">Course:</p>
      </div>
      <div class="nhsuk-grid-column-three-quarters">
        <p class="nhsuk-lede-text nhsuk-u-font-weight-bold word-break">@Model.CourseName</p>
      </div>
    </div>

    @if (Model.CourseHasContent) {
      @foreach (var section in Model.Sections) {
        <partial name="_CourseSectionExpandable" model="section" />
      }
    } else {
      <p>No course content enabled.</p>
    }

  </div>
</div>
